apiVersion: v1
kind: Service
metadata:
  name: rapp-rf-service
  namespace: rapp
spec:
  type: ClusterIP
  selector:
    app: rapp-rf

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8005
