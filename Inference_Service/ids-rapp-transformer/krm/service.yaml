apiVersion: v1
kind: Service
metadata:
  name: rapp-transformer-service
  namespace: rapp
spec:
  type: ClusterIP
  selector:
    app: rapp-transformer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8005
