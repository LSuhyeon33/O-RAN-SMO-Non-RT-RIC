apiVersion: v1
kind: Service
metadata:
  name: rapp-baseline-bi-lstm-service
  namespace: rapp
spec:
  type: ClusterIP
  selector:
    app: rapp-baseline-bi-lstm
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8005
