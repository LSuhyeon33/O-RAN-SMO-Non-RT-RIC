apiVersion: v1
kind: Service
metadata:
  name: rapp-xgboost-service
  namespace: rapp
spec:
  type: ClusterIP
  selector:
    app: rapp-xgboost

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8005
