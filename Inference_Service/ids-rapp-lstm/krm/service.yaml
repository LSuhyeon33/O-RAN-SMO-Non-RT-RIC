apiVersion: v1
kind: Service
metadata:
  name: rapp-lstm-service
  namespace: rapp
spec:
  type: ClusterIP
  selector:
    app: rapp-lstm
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8005
